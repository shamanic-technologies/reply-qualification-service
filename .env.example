# Neon Database (reply-qualification-service)
REPLY_QUALIFICATION_SERVICE_DATABASE_URL='postgresql://...'

# Anthropic API - Platform key (used when no BYOK provided)
# Pressbeat uses this platform key
# MCPFactory users provide their own key via byokApiKey in request body
ANTHROPIC_API_KEY='sk-ant-...'

# Service-to-service auth
REPLY_QUALIFICATION_SERVICE_API_KEY='...'
